import { boot } from 'quasar/wrappers'
import 'swiper/css' // estilos básicos do Swiper
import 'swiper/css/navigation' // caso queira usar navegação
import 'swiper/css/pagination' // caso queira paginação

export default boot(() => {
  // Aqui você pode fazer configurações globais se precisar
  // Exemplo: importar módulos do Swiper e disponibilizar
})
